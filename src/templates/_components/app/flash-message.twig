{% set flashError = craft.app.session.getFlash('error') %}
{% set flashSuccess = craft.app.session.getFlash('success') %}

{% if flashError %}
	<div class="mb-4 p-4 text-sm text-red-700 bg-red-100 border border-red-300 {{ craft.fostercheckout.settings.branding.style|default('rounded') == 'flat' ? 'flat' : 'rounded-lg' }}">
		{{ flashError }}
	</div>
{% endif %}

{% if flashSuccess %}
	<div class="mb-4 p-4 text-sm text-green-700 bg-green-100 border border-green-300 {{ craft.fostercheckout.settings.branding.style|default('rounded') == 'flat' ? 'flat' : 'rounded-lg' }}">
		{{ flashSuccess }}
	</div>
{% endif %}
