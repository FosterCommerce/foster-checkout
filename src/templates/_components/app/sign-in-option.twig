<div class="grid grid-cols-1 gap-4">
	<a
		href="{{ actionUrl('foster-checkout/sign-in') }}"
		class="flex justify-center items-center w-full h-12 px-6 font-medium text-[var(--brandColor)] text-center bg-white border border-[var(--brandColor)] rounded-lg"
	>
		{{ 'email.signIn'|t('foster-checkout') }}
	</a>

	<span class="text-sm font-medium text-gray-500 text-center">
		{{ 'email.signInDivider'|t('foster-checkout') }}
	</span>
</div>
