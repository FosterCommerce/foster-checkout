{% if not craft.fostercheckout.settings.options.isSinglePageCheckout %}
	{% if currentUser %}
		{% redirect siteUrl(craft.fostercheckout.settings.paths.checkout) ~ '/address' %}
	{% else %}
		{% redirect siteUrl(craft.fostercheckout.settings.paths.checkout) ~ '/email' %}
	{% endif %}
{% endif %}

{% extends 'foster-checkout/checkout/single-page/index' %}
