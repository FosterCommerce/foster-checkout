{% if currentUser %}
	{% redirect siteUrl(craft.fostercheckout.settings.paths.checkout) ~ '/address' %}
{% else %}
	{% redirect siteUrl(craft.fostercheckout.settings.paths.checkout) ~ '/email' %}
{% endif %}
